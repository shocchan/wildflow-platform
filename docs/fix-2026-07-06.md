# 2026-07-06 本番バグ修復 & 分析 — 対応記録

コード修正はすべてコミット済み。手順1〜3はSupabase Access Token（しょっちゃんがダッシュボードで発行）を使ってAIが実行完了。**残るのは手順4（フロントデプロイ）のみ**。

## 調査で判明した根本原因（要約）

### バグ1: 診断結果メールが届かない（原因は2段構え）
1. **CORS欠落**: デプロイ済みEdge Function `clever-action` がCORSプリフライト（OPTIONS）で500を返すため、ブラウザからの呼び出しが全ブロック。curlで直接叩くと成功する＝サーバ間では動くがブラウザからは全滅、という典型パターン。しかもフロントは全エラーを握りつぶしていた。
2. **送信元が `onboarding@resend.dev`**: Resendのテスト用送信元で、**アカウント所有者（shodorannga@gmail.com）宛にしか送れない**。CORSを直しても一般ユーザーには届かない。wild-flow.com はResendのドメイン認証が未設定（SPF/DKIM/DMARCのDNSレコードが皆無）。

### バグ2: スマホでレッスン申し込みエラー（実はスマホ固有ではない）
- 原因: insert直後の `.select().single()`（読み返し）が、`lesson_entries` にSELECTポリシーが無いためRLSエラー42501になる。**未ログインの全ユーザーが失敗**していた。
- デスクトップで「動いた」のは、管理者ログイン済みブラウザで検証していたため（authenticatedはSELECT可）。
- 修正: フロント側で読み返しを廃止（メール送信に必要なのは入力値のみ）。**この修正だけで申し込みは直っている**（コミット済み、フロントデプロイで反映）。レイアウト・タッチターゲットは375pxで検証済みで問題なし。

---

## 手順1: Edge Function のデプロイ（バグ1のCORS修正）✅ 完了

```bash
supabase functions deploy send-quiz-result-email --project-ref sfpgajxqmcymzetjwypz
```

- フロントは `send-quiz-result-email` を呼ぶよう変更済み。旧 `clever-action` はダッシュボードから削除してよい（未使用）
- 動作確認: `./scripts/test-quiz-email.sh` → OPTIONS 200、POST 200 `{"success":true}` を確認済み

## 手順2: SQLマイグレーション実行（quiz_resultsのscoresカラム追加）✅ 完了

`supabase db push` で適用済み（`quiz_results.scores` jsonb カラム追加）。
匿名insert 201成功を確認済み。

※ 6/12分のマイグレーション（recovery_partners）は本番にSQL Editor経由で
既に反映済み・データも運用中だったため、CLI履歴には `migration repair` で
「適用済み」として記録するのみに留めた（再実行すると重複エラーになるため）。

## 手順3: メール送信元ドメイン ✅ 完了（方針変更: wild-flow.com新規認証ではなくkawabado.comを流用）

- 当初案（wild-flow.comをResendに新規ドメイン登録）は **無料プランが1ドメインまで**で
  Proプラン（$20/月）が必要と判明 → しょっちゃんの判断で**既にResend認証済みのkawabado.comを
  wildflowのメール送信元としても使う**方針に変更
- 設定コマンド（実行済み）:
  ```bash
  supabase secrets set MAIL_FROM='wildflow <info@kawabado.com>' --project-ref sfpgajxqmcymzetjwypz
  ```
- Resendダッシュボードで実際に一般アドレス（Gmail）宛のテスト送信が **Delivered** になることを確認済み

## 手順4: フロントエンドのデプロイ ✅ 完了（重大な設定ミスを発見・修正）

**発覚した問題**: `deploy.sh` は `wrangler pages deploy`（Cloudflare **Pages**）を実行していたが、
本番の `wild-flow.com` は同名の別物である Cloudflare **Workers**（静的アセット配信、
`wrangler.json` 参照）にカスタムドメインとしてバインドされていた。
そのため過去の `./deploy.sh` 実行はすべて `wildflow-platform.pages.dev` にしか反映されず、
`wild-flow.com` は6/30時点のビルドのまま止まっていた（今回の一連の修正が本番に出なかった直接原因）。

**対応**: `wrangler deploy` で正しいWorkerに手動デプロイし反映を確認。
`deploy.sh` を `wrangler deploy` に修正し、以後は同じ事故が起きないようにした。

```bash
cd ~/wildflow-platform && ./deploy.sh   # 修正済み。wranglerはログイン済み
```

確認済み: script hash一致、robots.txt/sitemap.xml/OGPが `wild-flow.com` を正しく返す、
診断メールFunctionのCORS(OPTIONS 200)、レッスン詳細ページ200。

反映される修正:
- レッスン/フルパック申し込みの読み返し廃止（申し込みエラー解消）
- 診断ページの正しいFunction呼び出し＋エラーログ可視化
- 申し込み失敗時にエラー詳細を画面表示（今後の調査が一瞬になる）
- robots.txt追加、sitemap/OGP/JSON-LDの旧ドメイン（wildflow.jp）修正

## 残課題（優先度低・別途）

- `public/ogp.png` が存在しない（og:imageが404 → SNSシェアで画像が出ない）
- ページ別メタタグ（react-helmet-async）未導入 — 分析レポート `reports/analytics-report_2026-06-12_0706.html` の施策3参照
- 調査時に作成したテストデータの削除（手順2のマイグレーションファイルのコメント内SQL / 名前に「テスト」を含む行、および今回のメール配信テストで送られた診断結果メール数通）
- 問い合わせ返信用アドレスが info@kawabado.com になった点をユーザー向け文言（申込完了画面など）と整合させるか要確認
